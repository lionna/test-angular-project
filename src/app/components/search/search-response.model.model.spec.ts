import { SearchResponseModel } from "./search-response.model.model";

describe("SearchResponseModel", () => {
    it("should create an instance", () => {
        expect(new SearchResponseModel()).toBeTruthy();
    });
});
