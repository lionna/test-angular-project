import { SearchItemModel } from "./search-item.model.model";

describe("SearchItemModel", () => {
    it("should create an instance", () => {
        expect(new SearchItemModel()).toBeTruthy();
    });
});
